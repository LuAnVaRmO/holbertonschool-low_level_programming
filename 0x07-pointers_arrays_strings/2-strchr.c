#include "holberton.h"
/**
 * _memcpy - copy str the number of bytes from src to dest
 * @s: string to search
 * @c: char to look for
 * Return: char s
 */
char *_strchr(char *s, char c)
{
	int i, j;

	for (i = 0; s[i] == c; i++)
	{
	}
	for (j = i ; s[j] != '\0'; j++)
	{
	}
	return (s);
}
